# NHS React + Tailwind Starter

Minimal Vite + React + Tailwind starter created in this folder.

How to run:

```bash
# install dependencies
npm install

# start dev server
npm run dev
```

Open http://localhost:5173 (or the port printed by Vite) to see "Hello, world!".
